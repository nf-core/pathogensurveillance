Changes in component 'nf-core/bakta/bakta'
'modules/nf-core/bakta/bakta/meta.yml' is unchanged
'modules/nf-core/bakta/bakta/environment.yml' is unchanged
Changes in 'bakta/bakta/main.nf':
--- modules/nf-core/bakta/bakta/main.nf
+++ modules/nf-core/bakta/bakta/main.nf
@@ -31,7 +31,7 @@
 
     when:
     task.ext.when == null || task.ext.when
-
+    
     script:
     def args = task.ext.args ?: ''
     prefix = task.ext.prefix ?: "${meta.id}"
@@ -39,11 +39,16 @@
     def prodigal_tf_opt = prodigal_tf ? "--prodigal-tf ${prodigal_tf[0]}" : ""
     def regions_opt = regions ? "--regions ${regions}" : ""
     def hmms_opt = hmms ? "--hmms ${hmms}" : ""
-
     """
     ## Fake home due to fontconfig 'no writeable cache directory' issue
     mkdir nxf_home
     export HOME=\$PWD/nxf_home
+
+    db_real="\$(realpath "${db}")"
+    if [ ! -e "\$db_real/amrfinderplus-db/latest" ] || [ ! -L "\$db_real/amrfinderplus-db/latest" ]; then
+        rm -rf "\$db_real/amrfinderplus-db/latest"
+        amrfinder_update --force_update --database "\$db_real/amrfinderplus-db"
+    fi
 
     bakta \\
         ${fasta} \\

'modules/nf-core/bakta/bakta/tests/main.nf.test' is unchanged
'modules/nf-core/bakta/bakta/tests/nextflow.config' is unchanged
'modules/nf-core/bakta/bakta/tests/main.nf.test.snap' is unchanged
************************************************************
